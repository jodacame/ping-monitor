{
  "name": "@ping/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed, validated configuration loaders (per concern) and the shared structured logger.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ping/core": "workspace:*",
    "pino": "^9.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
