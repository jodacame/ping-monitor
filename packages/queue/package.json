{
  "name": "@ping/queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Event-driven transport over Redis Streams: the per-region check queue and the domain event bus.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ping/config": "workspace:*",
    "@ping/core": "workspace:*",
    "ioredis": "^5.4.0"
  }
}
