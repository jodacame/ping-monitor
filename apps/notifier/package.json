{
  "name": "@ping/notifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Consumes monitor status-change events and dispatches alerts via notification connectors.",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ping/config": "workspace:*",
    "@ping/core": "workspace:*",
    "@ping/db": "workspace:*",
    "@ping/notifications": "workspace:*",
    "@ping/queue": "workspace:*"
  }
}
