{
  "name": "@ping/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "REST API: authentication, workspaces, monitors, statistics.",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "seed": "tsx src/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@ping/checks": "workspace:*",
    "@ping/config": "workspace:*",
    "@ping/core": "workspace:*",
    "@ping/db": "workspace:*",
    "@ping/notifications": "workspace:*",
    "@ping/queue": "workspace:*",
    "fastify": "^5.2.0",
    "jose": "^5.9.0",
    "zod": "^3.24.0"
  }
}
